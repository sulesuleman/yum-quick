# Base URL for the same JSON Server fake backend the mobile app uses (apps/mobile/db.json),
# started via `npm run mobile:mock-api` from the repo root.
# Point this at the SAME running instance the mobile app uses so both apps read/write the
# same data and stay in sync — not a separate copy.
VITE_API_URL=http://localhost:3001
